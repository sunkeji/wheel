<manifest xmlns:android="http://schemas.android.com/apk/res/android"

    package="com.skj.wheel">

    <application
        android:allowBackup="true"
        android:label="@string/app_name"
        android:supportsRtl="true">
        <activity android:name=".album.activity.AlbumActivity" />
        <activity android:name=".album.activity.ImageFileActivity" />
        <activity android:name=".album.activity.ShowAllPhotoActivity" />
        <activity android:name=".album.activity.ViewGalleryActivity" />
    </application>

</manifest>
